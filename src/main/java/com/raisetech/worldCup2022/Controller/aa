@PatchMapping("/participating-country/{id}")
    ResponseEntity<Map<String, String>> update(@PathVariable("id") int number, @RequestBody UpdateForm form) {
        //　更新処理は省略
        return ResponseEntity.ok(Map.of("message", "successfully updated"));
    }

 @PatchMapping("/participating-country/{continent}")
    ResponseEntity<Map<String, String>> updateContinent(@PathVariable("continent") String continent, @RequestBody UpdateForm form) {
        return ResponseEntity.ok(Map.of("message", "successfully updated"));
    }


     @PatchMapping("/participating-country/{id}")
        ResponseEntity<Map<String, String>> updateId(@PathVariable("id") int number, @RequestBody UpdateForm form) {
            return ResponseEntity.ok(Map.of("message", "successfully updated"));
        }
